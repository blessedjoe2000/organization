/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "unitedesanorganization1.s3.us-west-1.amazonaws.com",
      "76yw7v2l2z.ufs.sh",
    ],
  },
};

export default nextConfig;
